#ifndef IO_MACROS_H
#define IO_MACROS_H

// #define WRITE_STDOUT(str) write(STDOUT_FILENO, str, strlen(str))
// #define WRITE_STDERR(str) write(STDERR_FILENO, str, strlen(str))

// #define WRITE_CONST_STR_STDOUT(str) write(STDOUT_FILENO, str, sizeof(str))
// #define WRITE_CONST_STR_STDERR(str) write(STDERR_FILENO, str, sizeof(str))

#endif